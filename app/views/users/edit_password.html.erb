<%= form_for @user do |f| %>
  <%= f.label :password %>
  <%= f.password_field :password, class: "new-input" %>

  <%= f.label :password_confirmation %>
  <%= f.password_field :password_confirmation, class: "new-input" %>

  <%= f.submit 'Change Password' %>
<% end %>
